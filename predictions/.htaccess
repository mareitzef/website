Options +ExecCGI
AddHandler wsgi-script .wsgi

RewriteEngine On
RewriteBase /predictions/

# Do NOT send static files through WSGI
RewriteCond %{REQUEST_URI} !^/predictions/static/

# Send everything else to WSGI
RewriteRule ^(.*)$ predictions.wsgi [QSA,PT,L]
